import * as React from 'react'

// render the `icon` simply as `children`, must allow accepting refs
export function AccessTooltipIcon(props: React.PropsWithChildren<{ title: string | React.ReactElement }>): React.ReactElement
