---
docModule:
    package: '@ui-schema/ui-schema'
    modulePath: "ui-schema/src/"
    files:
        - "schemaTypeIs/*"
        - "schemaTypeIsDistinct/*"
        - "schemaTypeToDistinct/*"
---

# Schema Type Test Helper

These functions implement the default logic on deciding what a `type` keyword represents.

See [concepts about schema type and matching](/docs/widgets-composition#example-for-multiple-types) for more.
